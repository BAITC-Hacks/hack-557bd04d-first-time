# Локальное распознавание речи — работает офлайн, ничего не уходит наружу.
vosk>=0.3.45
imageio-ffmpeg>=0.5

# Локальное разделение на говорящих (pyannote + titanet в ONNX).
sherpa-onnx>=1.13
numpy>=1.24

# Разбор совещания. Без ключа проект работает в демо-режиме на готовом примере.
anthropic>=1.0
